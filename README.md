# Setup virtual environment

```bash
# Create virtual env
python -m virtualenv venv
# Activate virtual env
source venv/bin/activate
#You should see a `(venv)` appear at the beginning of your terminal prompt indicating that you are working inside the `virtualenv`.
# Deactivate virtual env once you are done
deactivate
```

# Environment variables

```bash
export GOODDATA_HOST="<GOODDATA_HOST>"
export GOODDATA_TOKEN="<GOODDATA_TOKEN>"
export GOODDATA_WORKSPACE_ID="<GOODDATA_WORKSPACE_ID>"
export GOODDATA_VISUALIZATION_ID="<GOODDATA_VISUALIZATION_ID>"
export GOODDATA_DASHBOARD_ID="<GOODDATA_DASHBOARD_ID>"

export EMAIL_PASSWORD="<EMAIL_PASSWORD>"
```
